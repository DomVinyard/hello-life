<!-- generated-by-life -->
# Generated by Life

This file is generated from `.life` files. Do not edit directly.
To change, edit the relevant `.life` file and run `./.life.core reconcile`.

## api

Backend service with a database

Commands:
- `dev`: `echo "starting api dev..."`
- `test`: `echo "tests passed"`
- `migrate`: `echo "migration complete"` (requires approval)
- `health`: `echo "healthy"`

# API

Backend service. Depends on blog.preview for content.
Secrets: DATABASE_URL, API_KEY (set via provider).

## blog

Static site that builds to HTML

Commands:
- `build`: `echo "building blog..."`
- `dev`: `echo "starting dev server..."`

# Blog

A static site. Builds to dist/. Deploy is push-to-main.

## evtest

tracks named events, totals via KV, recent rows via D1

Commands:
- `test`: `echo "evtest test passed"`

# evtest

Tracks named events. POST /event with Bearer token writes a row + bumps a KV counter.

## hello

Tiny Worker that proves the Life deploy loop

Commands:
- `test`: `echo "hello test passed"`

# Hello

A minimal Cloudflare Worker. The simplest thing
that can be deployed through Life.

## store

Test directory exercising KV, D1, and R2 bindings

Commands:
- `test`: `echo "store test passed"`

# store

Test directory exercising the KV, D1, and R2 infra bindings.

